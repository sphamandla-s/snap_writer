import 'package:flutter/material.dart';

class Simplify extends StatelessWidget {
  const Simplify({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('You current have not simplify'),);
  }
}
